{
  "name": "@authently/ai",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./transcription": "./src/transcription/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint not yet wired (Sprint 01, Step 4)' && exit 0",
    "test": "echo 'no non-transcription tests yet (run pnpm test:ai for the transcription suite)' && exit 0",
    "test:ai": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^5.22.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
