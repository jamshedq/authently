# Sprint 07 C2a — pinned versions per SPRINT_07_preflight.md Item 2.
# Both libraries support Python >=3.8; Trigger.dev v4 pythonExtension
# creates the venv at /opt/venv inside the deployment container, with
# the runtime version determined by the deployment image (verify via
# `--log-level debug --dry-run` if a tighter floor is needed).
trafilatura==2.0.0
pdfplumber==0.11.9

# Sprint 08 B2 — yt-dlp for YouTube audio extraction (per A2.1
# (iii)+(α) lock: hard pin + weekly automated bump PR + manual smoke
# on every bump). Bump cadence: weekly Monday 14:00 UTC via
# .github/workflows/yt-dlp-bump.yml. Manual smoke-on-bump checklist
# at docs/runbooks/yt-dlp.md "Manual bump procedure" section.
yt-dlp==2025.10.14
