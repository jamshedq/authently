name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Node 20 deprecation note (2026-04):
# actions/checkout@v4, pnpm/action-setup@v4, and actions/setup-node@v4 still
# bundle Node.js 20 as their JS runtime and emit deprecation warnings.
# Warnings are non-blocking; revisit when these actions ship Node 24 builds
# (likely actions/checkout@v5 etc.). Opting in early via
# `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` is intentionally NOT set —
# upstream actions haven't certified Node 24 yet, and a forced bump would
# risk breaking CI for a cosmetic warning.

jobs:
  ci:
    name: lint + typecheck + test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        # Version is read from the `packageManager` field in root package.json.
        # Setting it here as well triggers a "multiple versions specified" error.
        uses: pnpm/action-setup@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: License headers
        run: pnpm test:license-headers

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Set up Python
        # Sprint 07 C2.5 — pytest gate for apps/jobs/python/ extraction
        # modules. Pinned to 3.12 as a CI-side approximation; production
        # runtime version is determined by Trigger.dev's deployment
        # container and is not pre-flight-pinned (see SPRINT_07_preflight.md
        # Item 1 [NOTE 2026-05-06]). Both extraction libraries support
        # Python >=3.8, so 3.12 exercises the modules under a current
        # stable interpreter.
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python dependencies
        # No pip caching here: the broader workflow does not currently
        # cache pip, and introducing it for one gate would establish a
        # new convention. Revisit alongside any future pip-caching
        # decision rather than as a side-effect of C2.5.
        run: |
          pip install --upgrade pip
          pip install -r apps/jobs/python/requirements.txt
          pip install -r apps/jobs/python/dev-requirements.txt

      - name: Tests (Python)
        run: pnpm test:python

  rls:
    name: RLS isolation tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        # Version is read from the `packageManager` field in root package.json.
        # Setting it here as well triggers a "multiple versions specified" error.
        uses: pnpm/action-setup@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Start Supabase
        run: supabase start

      - name: Capture local Supabase env
        run: |
          STATUS=$(supabase status -o env)
          {
            echo "SUPABASE_URL=$(echo "$STATUS" | grep '^API_URL=' | cut -d= -f2- | tr -d '\"')"
            echo "SUPABASE_ANON_KEY=$(echo "$STATUS" | grep '^ANON_KEY=' | cut -d= -f2- | tr -d '\"')"
            echo "SUPABASE_SERVICE_ROLE_KEY=$(echo "$STATUS" | grep '^SERVICE_ROLE_KEY=' | cut -d= -f2- | tr -d '\"')"
          } >> "$GITHUB_ENV"

      - name: Verify types.ts is up to date
        # Regenerates types from the live local Supabase schema and fails if
        # the result differs from what's committed. Catches schema-vs-types
        # drift at PR time. Lives in this job (not the lint/typecheck job)
        # because it requires a running Supabase instance.
        run: |
          pnpm --filter @authently/db gen:types
          git diff --exit-code packages/db/types.ts \
            || (echo "::error::packages/db/types.ts is stale. Run 'pnpm db:gen-types' locally and commit the result." && exit 1)

      - name: RLS tests
        run: pnpm test:rls

      - name: Auth integration tests
        # Exercises the password-reset flow end-to-end against the local
        # Supabase + Inbucket instance (port 54324). Lives in this job
        # for the same reason as the RLS tests: needs a running Supabase.
        run: pnpm test:auth
